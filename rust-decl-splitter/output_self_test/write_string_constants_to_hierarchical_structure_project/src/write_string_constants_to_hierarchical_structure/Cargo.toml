[package]
name = "write_string_constants_to_hierarchical_structure"
version = "0.1.0"
edition = "2021"

[dependencies]
prelude = { path = "../../prelude" }
serde = { version = "1.0", features = ["derive"] }
