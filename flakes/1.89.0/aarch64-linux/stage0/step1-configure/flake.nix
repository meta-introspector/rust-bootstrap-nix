{ \ n  description = "Test flake for rustc 1.89.0";\ n\n  inputs = { \ n    nixpkgs.url = "github:meta-introspector/nixpkgs?ref=feature/CRQ-016-nixify";\ n  };\n\n  outputs = { self, nixpkgs }: \n    let\n      pkgs = import nixpkgs {\n        system = "aarch64-linux";\ n      };\n      # This rustcPath is the *source* rustc used to build the next stage\n      rustcPath = "/nix/store/b29wwnvfjfzkf23l2d077nmw5cncaz5s-rustc-1.84.1-aarch64-unknown-linux-gnu/bin/rustc";\n    in\n    {\n      devShells.aarch64-linux.default = pkgs.mkShell {\n        buildInputs = [\n          pkgs.cargo\n        ];\n        RUSTC = rustcPath;\n      };\n    };\n}
