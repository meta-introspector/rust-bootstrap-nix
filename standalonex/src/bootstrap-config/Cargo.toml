[package]
name = "bootstrap-config"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_derive = "1.0"
toml = "0.5"
bootstrap-config-macros = { path = "./config_macros" }
