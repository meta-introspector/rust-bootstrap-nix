use crate::parsed_config::ParsedConfig;
use crate::local_toml_config::LocalTomlConfig;
pub fn parse_inner_stage0(config: &mut ParsedConfig, toml: &LocalTomlConfig) {
    // Removed build_helper dependency
}
